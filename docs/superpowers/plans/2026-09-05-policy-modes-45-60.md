# Policy matrix and per-order routing implementation plan

> For agentic workers: execute with superpowers:executing-plans, TDD and independent code review.

**Goal:** replace the global 75-day premise with the approved 45/60 matrix and evidence-based per-order actions.
**Architecture:** keep policy selection pure, project official event evidence, then route before any write is scheduled. Existing tenant-scoped repositories and private runtime audit carry production evidence.
**Tech Stack:** PHP, MySQL, Node Windows bridge, systemd, GitHub CI.
**Spec:** docs/superpowers/specs/2026-09-05-policy-modes-45-60.md

## Global constraints
All external write flags stay false until business acceptance. Preserve main's existing draft in a separate commit before restoring a clean deployment checkout. Do not change legacy runtime or expose financial data on public endpoints.

## Task 1: matrix and invariant repair
Files: PolicySeeder.php, PolicyEngine.php, ShadowAuditRepository.php, scripts/shadow-audit.php, scripts/verify-migration.sh, tests/*policy*, tests/amazon-returns-runtime-test.php, docs.
- [ ] Add failing matrix tests for STANDARD45, pre-cutover Onsite/DBA45, post-cutover60, refund basis, unknown program/date and numeric version precedence.
- [ ] Run `php tests/policy-matrix-production-test.php` and record the failed assertions.
- [ ] Implement the matrix, shared exact validation and safe selection. Retain prior policy evidence before replacing active values.
- [ ] Run every `tests/*.php`, PHP/Node/shell syntax, tenant SQL audit and `git diff --check`.
- [ ] Review, commit, push and merge only the tested SHA.

## Task 2: evidence and routes
Files: ReturnActionRouter.php, Projector.php, SafeTDecisionEngine.php, daemon.php, RuntimeAudit.php, tests/return-action-router-test.php.
- [ ] Add red tests for existing review WAIT, explicit promises before/after deadline, no double support, classic FBA, lost/refused/damaged, return-in-transit, physical discrepancy, unknown evidence and expired/missing appeal deadline.
- [ ] Implement `SvAmazonReturnActionRouter::decide(array $case,array $timeline,array $policy,DateTimeImmutable $now): ?array`; null delegates unchanged claim lifecycle.
- [ ] Integrate at the decision boundary; use existing write action identifiers and non-write CHECK_FINANCES/HUMAN_REVIEW routes, with evidence keys and next_action_at.
- [ ] Run full tests, independent review and CI, then merge the tested SHA.
